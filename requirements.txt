# Core ML pipeline (src/) — tested on Python 3.11–3.13.
# Version floors are conservative; the pipeline also runs on current releases
# (verified on numpy 2.x / pandas 3.x / scikit-learn 1.9).
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=1.7          # cardiovascular regression ensemble; needs libomp on macOS (`brew install libomp`)
joblib>=1.3
matplotlib>=3.7
PyYAML>=6.0           # config loading (configs/default.yaml)

# Testing
pytest>=7.4

# Notebooks only (CNN exploration in notebooks/); not required by src/.
# Install separately if you want to run the TensorFlow notebooks:
#   pip install "tensorflow>=2.13" seaborn
